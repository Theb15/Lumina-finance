'use client';
import { FormEvent, useState } from 'react'; import { useRouter } from 'next/navigation'; import Link from 'next/link'; import { supabase, signInWithGoogle } from '@/lib/auth'; import { useAuth } from '@/components/AuthProvider'; import { registerLocalAccount } from '@/lib/local-auth';
export default function Signup(){const router=useRouter(); const {localMode}=useAuth(); const [name,setName]=useState('');const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const [busy,setBusy]=useState(false);
 async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError('');try{if(!supabase){if(password.length<8){setError('Use at least 8 characters for your password.');return;}const r=await registerLocalAccount(email,password,name);if(r.error){setError(r.error);return;}localStorage.setItem('lumina_local_email',r.account!.email);localStorage.setItem('lumina_local_name',r.account!.name);window.location.href='/';return;}const {data,error}=await supabase.auth.signUp({email,password,options:{data:{full_name:name},emailRedirectTo:`${window.location.origin}/auth/callback`}});if(error)setError(error.message);else if(data.session)router.push('/');else setError('Check your email to confirm your account.')}finally{setBusy(false)}}
 async function google(){const r=await signInWithGoogle();if(r.error)setError(localMode?'Google login requires Supabase configuration in .env.local.':r.error.message)}
 return <div className="auth-page"><div className="auth-shell"><div className="auth-visual"><div className="brand"><div className="brand-mark">✦</div><span>Lumina</span></div><div><h1>Build a calmer<br/><span style={{color:'var(--green)'}}>financial life.</span></h1><p>Bring your income, spending, and goals into one beautiful workspace. Then let Lumina help you plan the next move.</p></div><span className="eyebrow">Your data · Your goals · Your plan</span></div><div className="auth-form"><h2>Create your account</h2><p>Start with email/password, or use Google when Supabase is connected.</p>{error&&<div className="error-text" style={{marginBottom:14}}>{error}</div>}<form onSubmit={submit} className="auth-fields"><div className="field"><label>Name</label><input className="input" required value={name} onChange={e=>setName(e.target.value)} placeholder="Your name"/></div><div className="field"><label>Email</label><input className="input" type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com"/></div><div className="field"><label>Password</label><input className="input" type="password" minLength={8} required value={password} onChange={e=>setPassword(e.target.value)} placeholder="At least 8 characters"/></div><button className="button primary full-button" disabled={busy}>{busy?'Creating…':'Create account'}</button></form><div className="auth-divider">or</div><button className="button full-button" onClick={google}><span style={{fontWeight:700}}>G</span>&nbsp; Continue with Google</button>{localMode&&<p className="auth-note">Local mode stores a hashed password and per-user financial data in this browser. Configure Supabase later for real cloud auth and Google login.</p>}<p style={{fontSize:12,color:'var(--muted)',marginTop:24}}>Already have an account? <Link className="auth-link" href="/login">Sign in</Link></p></div></div></div>}
